#include<iostream>

using namespace std;

int main(){
    int one, two;
    cout <<"Please enter two integers: ";
    cin >> one >> two;
    cout <<"The product of " << one << " and " << two << " is " << one*two << "!";
}